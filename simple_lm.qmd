---
title: "First linear model"
---

from: https://vasishth.github.io/bayescogsci/book/ch-compbda.html#sec-simplenormal

```{r}
pacman::p_load(bcogsci)
```


```{r}
data("df_spacebar")
df_spacebar
```

# A simple linear model (Section 3.2.1)

```{r}
ggplot(df_spacebar, aes(t)) +
  geom_density() +
  xlab("Finger tapping times") +
  ggtitle("Button-press data")
```

```{r}
fit_press <- brm(t ~ 1,
  data = df_spacebar,
  family = gaussian(),
  prior = c(
    prior(uniform(0, 60000), class = Intercept, lb = 0, ub = 60000),
    prior(uniform(0, 2000), class = sigma, lb = 0, ub = 2000)
  ),
  chains = 4,
  iter = 2000,
  warmup = 1000
)
```

```{r}
as_draws_df(fit_press) %>%
  head(3) 
```

```{r}
fit_press
```

```{r}
as_draws_df(fit_press)$b_Intercept %>% mean()
```





